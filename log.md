# Log

## [2026-07-30] develop | rifatto il fix "WHERE non rilevato" dopo il riallineamento a raw
Riscrivendo `develop/tests/generic/` a partire dallo stato in `raw/` (voce precedente), è tornato anche il bug già trovato e corretto in precedenza: la guardia `if 'WHERE' in sql_upper` (o `l1_sql | upper`) controlla il testo maiuscolizzato, ma poi `l1_sql.split('WHERE')[1]` fa lo split sul testo **originale**, case-sensitive — un `where` scritto minuscolo in L1 risulta rilevato dalla guardia ma non trovato dallo split, `[1]` va fuori indice. Utente ha confermato il problema è di nuovo presente in `try_cast_from_sql.sql`. Riapplicato lo stesso fix a indice (`where_idx = sql_upper.find('WHERE')`, poi `l1_sql[where_idx + 5:]`) nei 4 file che condividono questo pattern: `try_cast_from_sql.sql`, `try_cast_positional.sql`, `primary_key_positional.sql` (su `where_clause_l1`), `primary_key_from_sql.sql`. Non ancora portato upstream in `raw/dwh-code/` né nella repo live `dwh-x-dbt`.

## [2026-07-30] develop | tutti i test generici riscritti da raw, tolto where_clause come argomento ovunque
Su richiesta dell'utente, riscritti tutti e 6 i file in `develop/tests/generic/` a partire dallo stato corrente in `raw/dwh-code/` (quello ripristinato dall'utente, con il lookup `selectattr('name','equalto',...)|first` originale, non il loop case-insensitive di questa sessione), togliendo `where_clause` come parametro esplicito ovunque compariva, e ripulendo i commenti (tenuti solo quelli strettamente necessari).
- `try_cast.sql`, `primary_key.sql`: avevano `where_clause` solo come argomento — tolto parametro e i suoi usi, nessun filtro di sostituzione.
- `try_cast_from_sql.sql`, `try_cast_positional.sql`: non avevano un parametro `where_clause` in `raw/` (solo un `where_clause` locale auto-rilevato dal `WHERE` nel `raw_code` L1) — copiati identici da `raw/`, nessuna modifica reale.
- `primary_key_positional.sql`: aveva **entrambi** in `raw/`, il parametro `where_clause` e il derivato `where_clause_l1` — tolto solo il parametro, tenuto `where_clause_l1`.
- `primary_key_from_sql.sql` (non esiste in `raw/`, nuovo test): riscritto sullo stesso pattern di `try_cast_from_sql` — `where_clause` locale auto-rilevato dal `raw_code` L1, nessun parametro.
Conseguenza: i 5 fix `where_clause` scritti in precedenza per `BSN/granulaires_v0/v1/vf`, `BSN/flux_cre_provision`, `POSTE/mailc_esiti_tgb` (che passavano `where_clause` come argomento a `try_cast_from_sql`) sono diventati inutili — quel test rileva già da solo il `WHERE` dal `raw_code` L1 — e i relativi 5 file in `develop/models/L0/` sono stati rimossi (tornati identici a `raw/`, nessuna proposta reale). Aggiornato `index.md`. Non ancora portato upstream in `raw/dwh-code/` né nella repo live `dwh-x-dbt`.

## [2026-07-30] develop | primary_key_from_sql riscritto sulla baseline "vecchia che funziona"
L'utente ha ripristinato in `raw/dwh-code/tests/generic/` lo stato precedente ai fix di questa sessione (lookup `graph.nodes.values() | selectattr('name','equalto',...) | first`, case-sensitive, senza il loop case-insensitive; `try_cast_from_sql`/`try_cast_positional` senza parametro `where_clause`, con il parsing case-sensitive del `WHERE` da `raw_code`; `primary_key`/`primary_key_positional` con `where_clause` come argomento) perché a quanto riferito è quello che funziona nel progetto reale. Riscritto `develop/tests/generic/primary_key_from_sql.sql` per rispecchiare esattamente questa baseline invece della versione precedente (che usava il lookup case-insensitive): lookup identico a `primary_key.sql` corrente in `raw/`, `where_clause` come argomento (non derivato dall'L1), `cast_failed_pks` che riusa il parsing del `raw_code` di `try_cast_from_sql` per l'espressione di cast reale delle colonne PK. Non ancora verificato se gli altri 5 file in `develop/tests/generic/` (che hanno ancora il fix case-insensitive di questa sessione) vadano riallineati allo stesso modo — chiesto esplicitamente all'utente prima di toccarli.

## [2026-07-30] develop | where_clause per i modelli L1 non-OCS con WHERE reale
Su richiesta dell'utente, cercati (grep su `raw/dwh-code/models/L1/`, esclusa OCS) tutti i modelli L1 con una clausola `WHERE` reale (non dentro una CTE su dati già derivati) su colonna sorgente diretta. Trovati: `BSN/granulaires_v0.sql`, `granulaires_v1.sql`, `granulaires_vf.sql` (`WHERE enreg = '01'`), `BSN/flux_cre_provision.sql` (`WHERE energ = '01'`), `POSTE/mailc_esiti_tgb.sql` (`WHERE TRY_TO_DATE(DT_ACCETTAZIONE,'DDMMYYYY') = {{ get_dt_osservazione('weekly') }}`). Esclusi su indicazione dell'utente: `SWORD/master_data.sql` (i suoi `WHERE` sono dentro CTE su XML flattenato, non su colonne sorgente dirette, e comunque la tabella non ha alcun test). Notato ma non corretto: `CDE/cde.sql` ha un `WHERE LTRIM(value) LIKE 'IO%'` reale ma zero `data_tests` in `cde_source.yml` — non c'è un test esistente a cui aggiungere `where_clause` (servirebbe prima aggiungere il test stesso, gap già segnalato nell'audit).
Scritti in `develop/models/L0/BSN/` (`granulaires_v0_source.yml`, `granulaires_v1_source.yml`, `granulaires_vf_source.yml`, `flux_cre_provision_source.yml`) e `develop/models/L0/POSTE/mailc_esiti_tgb_source.yml`: aggiunto `where_clause` come `arguments:` del test `try_cast_from_sql` già presente, riprendendo verbatim la condizione dell'L1. Per `mailc_esiti_tgb`, la condizione potrebbe cambiare se la proposta di reclassificazione a cluster D già in `develop/models/L1/POSTE/mailc_esiti_tgb.yml` (che sostituisce `get_dt_osservazione('weekly')` con `get_dt_accettazione()`) viene applicata upstream prima di questo fix — da riallineare in quel caso. Aggiornato `index.md`. Non ancora portato upstream in `raw/dwh-code/` né nella repo live `dwh-x-dbt`.

## [2026-07-30] develop | try_cast/try_cast_from_sql/try_cast_positional: fix "No first item, sequence was empty" per GROUP (SFC)
Utente ha segnalato l'errore Jinja "No first item, sequence was empty." sul test `try_cast_from_sql` applicato a `raw/dwh-code/models/L0/SFC/group_source.yml`. Causa: tutti e 3 i test generici in `raw/dwh-code/tests/generic/` (`try_cast.sql`, `try_cast_from_sql.sql`, `try_cast_positional.sql`) condividono le stesse righe 5-9 per individuare il nodo dbt del modello L1 corrispondente alla sorgente L0: `l1_model = model.identifier | replace('_source', '')`, poi `graph.nodes.values() | selectattr('name', 'equalto', ...) | first` (case-sensitive) prima su `stg_<l1_model>` poi su `<l1_model>`. Per la tabella `GROUP` (SFC), il nome della sorgente L0 è forzato in maiuscolo in `group_source.yml` (`name: GROUP`, `quoting.identifier: true`) perché `GROUP` è keyword riservata in Snowflake e richiede quoting — ma il modello L1 corrispondente è `raw/dwh-code/models/L1/SFC/group.sql`, il cui nodo dbt si chiama `group` (minuscolo, dal nome file). Il confronto `equalto` case-sensitive tra `"GROUP"` e `"group"` non trova mai un match (né su `stg_GROUP` né su `GROUP`), quindi `first` su una sequenza vuota restituisce un `Undefined` il cui messaggio è esattamente "No first item, sequence was empty.", che esplode al primo accesso ad attributo (`l1_node.columns`/`l1_node.schema`).
Fix: sostituita la coppia `selectattr(...) | first` con un loop esplicito case-insensitive su `graph.nodes.values()`, confrontando `n.name` con `l1_model | lower` (assumendo — verificato — che tutti i file `.sql` in `models/L1/` abbiano nomi tutto minuscolo, quindi `n.name` è già lowercase per costruzione). Scritti `develop/tests/generic/try_cast.sql`, `try_cast_from_sql.sql`, `try_cast_positional.sql` (le tre copie condividono lo stesso identico bug/fix nelle righe 5-9, nessun'altra riga toccata). Non è un problema isolato a `GROUP`: qualunque tabella SFC/OCS il cui nome fisico sia una keyword riservata (quindi forzata maiuscola/quotata nel source yml) avrebbe lo stesso problema.

Utente ha poi chiesto se anche i test `primary_key` funzionano: verificato che `primary_key.sql` e `primary_key_positional.sql` condividono le identiche righe 6-10 (stessa ricerca case-sensitive del nodo L1) — stesso bug, stessa esposizione per `GROUP`/tabelle con nome riservato. Applicato lo stesso fix (loop case-insensitive) in `develop/tests/generic/primary_key.sql` e `primary_key_positional.sql`. In totale 5 test generici toccati: `try_cast`, `try_cast_from_sql`, `try_cast_positional`, `primary_key`, `primary_key_positional`.

## [2026-07-30] develop | nuovo test primary_key_from_sql + audit completo no-OCS L0/L1 (coerenza PK/try_cast)
Dopo l'audit sistematico di tutte le 17 aree no-OCS (16 agent in background su ADOBE, BANCO, BSN, CDE, CRIF, CRUSCOTTI, CTC, DIL, FEA×2, IDM, MICRODATA, POSTE, PRIMEWEB, SAP, SFC, SWORD, XEROX — verifica: per ogni sorgente L0, il test PK va presente se e solo se l'L1 dichiara `constraints: primary_key`; la variante try_cast (`try_cast`/`try_cast_from_sql`/`try_cast_positional`) deve rispecchiare lo stile di cast reale in L1), l'utente ha notato che `primary_key.sql` ha lo stesso problema strutturale di `try_cast` vs `try_cast_from_sql`: la sua `cast_failed_pks` ricostruisce a mano `TRY_CAST(col AS tipo_dichiarato_in_L1)`, assumendo che il cast della colonna PK sia sempre semplice — sbagliato quando la colonna PK ha logica particolare (es. `TRY_TO_DATE`/`TRY_TO_TIMESTAMP` con formato custom, come `dt_accettazione` in POSTE o `itemdate`/`onlineperagosdataora`/`dataoraconsegnata` in XEROX).
Scritto `develop/tests/generic/primary_key_from_sql.sql`: `null_pks`/`duplicate_pks` invariate rispetto a `primary_key.sql` (operano sui valori grezzi della sorgente, non serve parsing); `cast_failed_pks` riusa la tecnica di parsing del `raw_code` L1 di `try_cast_from_sql` per estrarre l'espressione di cast reale per ciascuna `pk_column`, testando `({{ col.expr }}) is null` invece di `try_cast(col.name as data_type) is null`.
Effetto collaterale dell'audit: **XEROX** (`xerox_sla`) usa oggi `primary_key` (semplice) su `pk_columns` che includono 3 colonne con `TRY_TO_TIMESTAMP(...,'DD/MM/YYYY HH24:MI')` — il test attuale ricasta alla cieca con `TRY_CAST` invece di validare l'espressione reale, quindi va convertito a `primary_key_from_sql`. Anche il fix da fare per **POSTE** (`mailc_esiti_tgb`, PK mancante — vedi audit precedente) deve usare `primary_key_from_sql` fin da subito, non `primary_key` semplice, perché `dt_accettazione` (una delle due colonne PK) usa `TRY_TO_DATE(TRIM(...),'DDMMYYYY')`.
Aggiornato `index.md`. Non ancora portato upstream in `raw/dwh-code/` né nella repo live `dwh-x-dbt`.

**Correzione stessa giornata**: l'utente ha notato che `primary_key_from_sql.sql` teneva `where_clause` solo come parametro esplicito del test (come il vecchio `primary_key.sql`), mentre `try_cast_from_sql`/`try_cast_positional` derivano **anche** un filtro dalla clausola `WHERE` del `raw_code` L1 stesso (se l'L1 scarta righe, i controlli sulla sorgente L0 devono restare sullo stesso perimetro, altrimenti righe mai processate da L1 producono falsi positivi). Verificato il precedente corretto per la famiglia `primary_key`: `primary_key_positional.sql` (fedele all'originale) tiene **entrambi** i meccanismi, `where_clause` (parametro) e `where_clause_l1` (derivato da L1), applicati insieme via `and (...)` in tutte e 3 le CTE. Allineato `primary_key_from_sql.sql` allo stesso pattern: aggiunto `where_clause_l1` derivato dal `raw_code` L1, applicato insieme a `where_clause` in `null_pks`/`duplicate_pks`/`cast_failed_pks`.

**Nota a margine**: durante questa correzione si è scoperto che tutti i file scritti in `develop/` in questa sessione (inclusi quelli di `variazioni_anagrafiche`/`log_run_results`/i 6 test generici) risultavano cancellati dal disco pur essendo presenti nell'ultimo commit (`git status` li segnava `D`, non in stage) — non per un'azione mia in questa conversazione. Ripristinati con `git restore -- develop/` (recupero non distruttivo dal commit, nessuna modifica persa). Causa della cancellazione non identificata (fuori dal controllo di questa sessione).

**Reintrodotto `where_clause` come parametro esplicito, in tutti i test (stessa giornata, dopo un giro contrario)**: l'utente ha segnalato che dopo la rimozione del parametro tutti i test `primary_key` base davano `'NoneType' object has no attribute 'columns'`, causa non individuata con certezza (il lookup di `l1_node` non era stato toccato da quella modifica). Su richiesta esplicita, tolta del tutto la logica di rilevamento del `WHERE` dal `raw_code` L1 (`where_idx`/`where_clause_l1`) in tutti e 6 i test, e ripristinato `where_clause=none` come argomento esplicito nella firma di ciascun test, usato direttamente nei `WHERE`/`and (...)` delle CTE — nessuna derivazione automatica dall'L1, il filtro va passato a mano nello yml quando serve.

**Nota (versione precedente, superata dal punto sopra)**: era stato rimosso `where_clause` come parametro: su richiesta esplicita dell'utente ("mai mai mai, nessun test"), tolto il parametro `where_clause` dalla firma e dall'uso di `try_cast.sql` e `primary_key.sql` (unici due che lo avevano come argomento vero e proprio). Per `primary_key_positional.sql`/`primary_key_from_sql.sql`, che avevano sia il parametro `where_clause` sia `where_clause_l1` derivato dall'L1, rimosso solo il parametro — `where_clause_l1` resta (non è "passato" da nessuno, è calcolato dal test stesso leggendo il `raw_code` dell'L1). `try_cast_from_sql.sql`/`try_cast_positional.sql` non avevano mai un parametro `where_clause` (solo la variabile locale derivata), quindi non toccati.

**Bug reale trovato dall'utente nella stessa parte**: l'estrazione del WHERE dell'L1 (`where_clause`/`where_clause_l1`) verificava `'WHERE' in sql_upper` (testo maiuscolizzato) ma poi faceva `l1_sql.split('WHERE')[1]` sul testo **originale**, case-sensitive. Se l'L1 scrive la clausola in minuscolo (`where`, comune nel progetto — es. `variazioni_stato_prat.sql`), la condizione risultava vera ma lo split sul testo originale non trovava alcun `'WHERE'` maiuscolo, la lista restava a un elemento e `[1]` andava fuori indice — il WHERE non veniva mai rilevato/estratto. Corretto in tutti e 4 i file che avevano questo pattern (`try_cast_from_sql.sql`, `try_cast_positional.sql`, `primary_key_positional.sql`, `primary_key_from_sql.sql`): sostituito con lo stesso approccio a indice già usato per `select_idx`/`from_idx` (`where_idx = sql_upper.find('WHERE')`, poi slice di `l1_sql` — non di `sql_upper` — da `where_idx + 5`), che funziona indipendentemente dal case con cui l'L1 scrive `WHERE`.

**Correzione successiva stessa giornata**: l'utente ha rieseguito `primary_key` su `GROUP` e ottenuto un nuovo errore, "'NoneType' object has no attribute 'columns'" — `l1_node` ancora `None` nonostante il fix. Causa: il primo fix confrontava solo `l1_model` lowercased contro `n.name` as-is (`n.name == l1_model`), non entrambi i lati. Se il nome reale del nodo dbt per il modello L1 è `GROUP` (maiuscolo — plausibile: Windows è case-insensitive, la copia vendorizzata locale può mostrare `group.sql` anche se il file committato nel repo live è `GROUP.sql`), il confronto falliva comunque. Corretto in tutti e 5 i file: `n.name | lower == ...` (case-insensitive su entrambi i lati, non solo su `l1_model`). Aggiornato `index.md`. Non ancora portato upstream in `raw/dwh-code/` né nella repo live `dwh-x-dbt`.

## [2026-07-30] develop | log_run_results: fix "unknown keyword None" (default() non copre l'esplicito None)
Utente ha segnalato l'errore Snowflake "unknown keyword None" durante `PARSE_JSON` nell'`on-run-end` hook. Causa: in `raw/dwh-code/macros/log/log_run_results.sql`, `{{ res.execution_time | default(0) }}` e `{{ res.failures | default("null") }}` usano il filtro Jinja `default()` nella sua forma a un argomento, che sostituisce **solo** i valori undefined — non un `None` esplicito. Per i nodi con `status = SKIPPED`, `res.execution_time` e `res.failures` sono attributi presenti ma valorizzati `None` (non undefined), quindi il filtro non scatta e nel JSON generato finisce il literal Python `None` (non quotato, diverso da `null`) — es. `"nm_execution_time": None` — non valido, da cui l'errore in `PARSE_JSON`. Il file `develop/` per questa macro non esisteva più (probabilmente ripulito in un resync precedente insieme ad altri fix già portati upstream, ma questo specifico non risulta applicato). Ricreato `develop/macros/log/log_run_results.sql`: entrambe le occorrenze passate a `default(x, true)` (forma a due argomenti, sostituisce anche i valori falsy incluso `None`) su `nm_execution_time` (righe MODEL e TEST) e `nm_failures` (riga TEST). `cd_query_sf`/`res.adapter_response.query_id` non toccato: l'attribute-access di Jinja su un oggetto `None` restituisce Undefined (non solleva l'attributo), quindi `default("")` lì funziona già correttamente. Aggiornato `index.md`. Non ancora portato upstream in `raw/dwh-code/` né nella repo live `dwh-x-dbt`.

## [2026-07-30] develop | variazioni_anagrafiche + variazioni_anagrafiche_day: conversione a S1 classica, senza union con la riga aperta nel target
Utente ha chiesto di riscrivere entrambi i modelli con la logica S1 "classica" (stesso pattern di `wfl_fase.sql`/`variazioni_stato_prat.sql`: `BASE`→`DEDUP` con `is_incremental_S1`, niente `UNION ALL` con la riga aperta nel target), mantenendo tutta la logica specifica di VAR_ANA (CASE `AL_DATA_INSERIMENTO` come `TS_INIZIO_VALIDITA` solo per il vero primo record, `PR_PK`/`PR_CONTROPARTE`).
Avevo inizialmente proposto di sostituire la union con una versione "snella" (solo colonne chiave) o con un update separato: l'utente ha corretto — non andava inventata una soluzione nuova, andava guardato come S1 è già gestito altrove nel codice. Verificato `variazioni_stato_prat.sql` (unico altro modello S1 su sorgente CDC-delta-only, non versionata in L1 come `wfl_fase`): la union non serve perché `BASE`/`FIN` rileggono **sempre tutta la storia** dalla sorgente (non solo il delta) — `TS_FINE_VALIDITA` e la QUALIFY di dedup sono quindi sempre ricalcolati corretti su tutta la storia a ogni run; `is_incremental_S1` si limita a un filtro (via `WHERE`) su quali righe, già corrette, vanno effettivamente riscritte nel target via merge sulla `unique_key` — incluse le righe precedentemente aperte che un evento successivo ha appena chiuso. Questo elimina anche il bisogno di `EXISTING_CODES`/`EX.CD_CONTROPARTE IS NULL`: con `BASE` che rilegge tutta la storia, `ROW_NUMBER()=1` per controparte individua già il vero primo record senza bisogno del check di esistenza nel target.
Riscritto `develop/models/L2/ANAGR_CONTROPARTE/variazioni_anagrafiche.sql` (sovrascritto il file preesistente, stessa struttura di fondo confermata equivalente — vedi log 2026-07-22): `BASE` (no split, no `EXISTING_CODES`) calcola anche `PR_PK` e una `TS_FINE_VALIDITA` "di appoggio" per `is_incremental_S1`; `DEDUP` applica hash+filtro incrementale; la SELECT finale ricalcola `TS_FINE_VALIDITA` (dopo il dedup hash) e `PR_CONTROPARTE` come semplice `ROW_NUMBER()` (nessun `OLD_PROGRESSIVO`, non più necessario). Un primo tentativo aveva una CTE `FIN` separata solo per la `TS_FINE_VALIDITA` di appoggio: su segnalazione dell'utente, accorpata in `BASE` (è solo una window function su colonne già presenti, Snowflake ammette il riferimento ad alias precedenti nella stessa SELECT list).
Scritto `develop/models/L2/ANAGR_CONTROPARTE/variazioni_anagrafiche_day.sql` (nuovo file, prima non presente in `develop/`) con lo stesso principio: `COMBINED`/`DAY` rileggono tutta la storia da `variazioni_anagrafiche` (non solo le (controparte, giorno) toccate dal delta); qui non esiste `HASHED_COLS` (l'unico dedup è "una riga per (controparte, giorno)" via `ROW_NUMBER`, come nell'originale), quindi non si può riusare `is_incremental_S1` così com'è — il filtro incrementale finale replica manualmente lo stesso criterio (LASTMODIFIEDDATA/boundary), adattato alla granularità DATE.
Aggiornato `index.md` (sezione Develop, entrambe le voci). Non ancora testato su dati reali, non ancora portato upstream in `raw/dwh-code/` né nella repo live `dwh-x-dbt`.

**Correzione stessa giornata**: l'utente ha segnalato `SELECT *` non ammesso (convenzione del progetto, vedi log 2026-07-22). Trovati due residui: `F.*` in `DEDUP` di `variazioni_anagrafiche.sql` (sostituito con lista esplicita) e `C.*` nella CTE `DAY` di `variazioni_anagrafiche_day.sql`. Per quest'ultimo, eliminata del tutto la CTE `DAY` (il suo `DT_FINE_VALIDITA` era comunque ricalcolato identico nella SELECT finale): `FROM DAY AS H` → `FROM COMBINED AS H`, e l'espressione `COALESCE(LEAD(...), TO_DATE('9999-12-31'))` duplicata nel filtro incrementale finale (un `WHERE` non può referenziare un alias della propria SELECT list).

**Secondo bug segnalato dall'utente, stessa giornata**: in `variazioni_anagrafiche.sql`, `DEDUP` chiamava `{{ is_incremental_S1(...) }}` che applica sia il filtro incrementale (`WHERE`) sia il collasso hash (`QUALIFY`) **prima** che la SELECT finale ricalcoli `TS_FINE_VALIDITA`/`PR_CONTROPARTE`. Poiché `PR_CONTROPARTE` è un conteggio sequenziale che deve considerare **tutta** la storia per controparte (non solo il batch incrementale corrente), filtrare a monte lo faceva ripartire da 1 a ogni run incrementale, invece di proseguire dal progressivo già assegnato in passato — la stessa classe di bug che il `ramo2`/`OLD_PROGRESSIVO` dell'originale esisteva per evitare. Fix: `DEDUP` ora fa **solo** il collasso hash (`QUALIFY HASHED_COLS IS DISTINCT FROM LAG(...)`) su tutta la storia, senza alcun filtro incrementale — così `TS_FINE_VALIDITA`/`PR_CONTROPARTE`, ricalcolati nella SELECT finale, vedono sempre l'intera sequenza storica. Il filtro incrementale (stessa logica di `is_incremental_S1`: `LASTMODIFIEDDATA` recente OR riga di confine appena chiusa) è stato spostato in un `QUALIFY` alla fine della SELECT finale, dopo tutti i join — referenzia direttamente gli alias `TS_FINE_VALIDITA`/`PR_CONTROPARTE` già calcolati nella stessa SELECT list (Snowflake lo ammette in `QUALIFY`, stesso meccanismo già usato da `HASHED_COLS` dentro `is_incremental_S1`). Verificato che `variazioni_anagrafiche_day.sql` non ha lo stesso problema: non ha alcun contatore globale tipo `PR_`, solo `DT_FINE_VALIDITA` via `LEAD` (relazione solo con la riga adiacente, non con tutta la storia), e il filtro era già applicato in fondo su `COMBINED` completo — nessuna modifica necessaria lì.

**Terza correzione, stessa giornata**: l'utente ha fatto notare che la soluzione precedente (QUALIFY spostato in fondo, `DEDUP` senza filtro) non era comunque equivalente all'originale — ripristinato `is_incremental_S1` in `DEDUP` (il filtro incrementale lì va bene: la finestra che porta dentro è comunque una coda contigua della storia per le controparti toccate). Il vero problema era nel calcolo di `PR_CONTROPARTE`: nell'originale, `H.OLD_PROGRESSIVO` è valorizzato **solo** sulla riga "esistente" riportata dal target via `ramo2` (`IS_EXISTING`), che quindi mantiene il proprio progressivo invariato tramite `COALESCE(H.OLD_PROGRESSIVO, ...)`; le righe nuove vengono numerate a parte (`ROW_NUMBER() PARTITION BY CD_CONTROPARTE, IS_EXISTING`) e sommate al vecchio massimo. Il mio primo tentativo di sostituire `OLD_PROGRESSIVO` con un lookup diretto su `{{ this }}` (`MAX(PR_CONTROPARTE) GROUP BY CD_CONTROPARTE`) perdeva questa distinzione: senza `ramo2`/`IS_EXISTING`, la riga di confine (riselezionata da `BASE`, che rilegge tutta la storia, solo per farle aggiornare `TS_FINE_VALIDITA` da `is_incremental_S1`) finiva mescolata nello stesso `ROW_NUMBER()` delle righe genuinamente nuove, venendo ricontata e corrompendo il progressivo già assegnato. Fix: aggiunta `MAX(TS_INIZIO_VALIDITA)` alla CTE `OLD_PROGRESSIVO` per ricostruire l'equivalente di `IS_EXISTING` per confronto (`D.TS_INIZIO_VALIDITA <= OP.OLD_TS_INIZIO_VALIDITA` ⇒ riga già esistente, mantiene `OP.OLD_PROGRESSIVO` invariato; altrimenti nuova, numerata a parte via `ROW_NUMBER() PARTITION BY CD_CONTROPARTE, (quel confronto)`), stessa struttura `COALESCE`/partizionamento dell'originale.

**Quarta correzione, stessa giornata**: rileggendo `variazioni_anagrafiche_day.sql`, trovato un bug distinto: il filtro incrementale finale era un `WHERE` che referenziava direttamente `LEAD(...) OVER (...)` — le window function non sono ammesse in `WHERE` in SQL (solo in `SELECT`/`ORDER BY`/`QUALIFY`), quindi il file non avrebbe compilato. Convertito in `QUALIFY` (che ammette window function, essendo valutato dopo di esse) e semplificato referenziando direttamente l'alias `DT_FINE_VALIDITA` già calcolato nella SELECT list, invece di duplicare l'espressione `COALESCE(LEAD(...), ...)` due volte.

**Quinta correzione, stessa giornata**: l'utente ha fatto notare che la struttura di `variazioni_anagrafiche_day.sql` era incoerente con quella di `variazioni_anagrafiche.sql`: quest'ultimo applica il filtro incrementale presto (dentro `DEDUP`, subito dopo `BASE`), mentre `day.sql` teneva `COMBINED` sempre non filtrato e rimandava tutto a un `QUALIFY` finale sull'intero storico ricalcolato. Ristrutturato per coerenza: `COMBINED` ora calcola anche `DT_FINE_VALIDITA` "di appoggio" (come `BASE` fa con `TS_FINE_VALIDITA`); una nuova CTE `FILTERED` applica subito il filtro incrementale (stessa logica di `is_incremental_S1`, adattata a grana DATE, senza hash perché qui il dedup resta solo "una riga per (controparte, giorno)"); la SELECT finale ricalcola `DT_FINE_VALIDITA` fresca sul set già filtrato (`FROM FILTERED AS H`), stesso principio del ricalcolo di `TS_FINE_VALIDITA` su `DEDUP` nell'altro file. Nota: la lista colonne esplicita di `FILTERED` (niente `SELECT *`) è stata scritta a mano riprendendo l'elenco di `COMBINED`.

**Sesta correzione, stessa giornata**: l'utente ha segnalato l'errore Snowflake "window function may not be nested inside another window function" in `variazioni_anagrafiche.sql` — stesso bug già risolto una volta nel fix del 2026-07-22 e ripresentatosi quando avevo accorpato la CTE `FIN` dentro `BASE` su richiesta dell'utente in un giro precedente di questa stessa giornata: `TS_INIZIO_VALIDITA` contiene già un `ROW_NUMBER()` (per il CASE del primo record storico), e calcolare `TS_FINE_VALIDITA` con `LEAD(TS_INIZIO_VALIDITA) OVER (...)` nella stessa SELECT list annida una window function dentro l'altra — a differenza del caso `HASHED_COLS`/`is_incremental_S1` (dove l'alias referenziato in `QUALIFY` non contiene esso stesso una window function), qui Snowflake non tollera il riferimento. Ripristinata la separazione in due CTE come nel fix originale: `BASE_RAW` calcola solo `TS_INIZIO_VALIDITA`/`PR_PK` (un livello di window function), poi `BASE` la rilegge come colonne semplici e vi calcola sopra `TS_FINE_VALIDITA` via `ts_fine_validita()` — nessun annidamento. `DEDUP` invariato (legge da `BASE`).

## [2026-07-28] develop | fix regressione generate_schema_name: hook on-run-start/on-run-end rompevano con la versione strict
Utente ha segnalato errore reale in run: `agos-on-run-start-0` falliva per "schema mancante". Causa: la versione strict di `generate_schema_name` (proposta il 2026-07-28) fa raise per qualunque nodo senza `custom_schema_name`, ma gli hook `on-run-start`/`on-run-end` di `dbt_project.yml` vengono compilati da dbt come nodi `resource_type: operation` (es. `agos-on-run-start-0`), che passano comunque per questa macro pur non avendo (e non potendo avere) uno schema custom. Corretto `develop/macros/generate_schema_name.sql`: il raise scatta solo se `node.resource_type in ('model', 'seed', 'snapshot')`; tutti gli altri resource_type (operation/hook inclusi) tornano al fallback normale su `target.schema`.

## [2026-07-28] develop | filtro SELECT allineato al target di delete_month/delete_week per cluster D
Discussione con l'utente su un caso limite: `mailc_esiti_tgb` (cluster D, `DT_ACCETTAZIONE`) cancella solo la settimana target (var/default) nel pre_hook, ma il `SELECT` non aveva alcun filtro — se in L0 arrivano righe con `dt_accettazione` vecchia (reprocessing), non vengono cancellate ma vengono comunque inserite, causando duplicati permanenti. Chiarito anche un mio errore di analisi precedente su `delete_month(get_dt_osservazione('ts_riferimento'))`: quel positional arg imposta `column` (primo parametro), non `date_expr` — si usa quando la tabella non ha `DT_OSSERVAZIONE` materializzata affidabile e va ricalcolata da `ts_riferimento` invece di usare il letterale colonna; non è un "cancella tutto", correzione rispetto a quanto avevo detto prima.
Fix applicati: (1) `develop/models/L1/POSTE/mailc_esiti_tgb.sql` — aggiunto `WHERE TRY_TO_DATE(DT_ACCETTAZIONE, 'DDMMYYYY') = {{ get_dt_accettazione() }}`, stesso date_expr del pre_hook `delete_week`. (2) `develop/macros/generate_models/generate_model.sql` — per cluster D, aggiunta `WHERE {{ get_dt_osservazione('ts_riferimento') }} = {{ get_dt_osservazione() }}` al SELECT generato, così delete e insert ragionano sempre sullo stesso periodo anche per i futuri archivi mensili generati da codegen.
Chiesto esplicitamente all'utente se estendere il codegen a supportare anche cadenza settimanale in generale (richiederebbe un nuovo campo periodicità in `TECH.CFG_L1_CLUSTER_STO`): **deciso di no per ora** — `mailc_esiti_tgb` resta un'eccezione scritta a mano, il codegen resta mensile-only.

## [2026-07-28] develop | generate_schema_name: versione strict, niente fallback silenzioso su target.schema
Utente ha chiesto se un modello senza `schema:` custom possa dare errore invece di finire silenziosamente nello schema personale (`target.schema`, comportamento di default dbt riprodotto da `raw/dwh-code/macros/generate_schema_name.sql`). Proposta versione strict in `develop/macros/generate_schema_name.sql`: se `custom_schema_name is none`, `exceptions.raise_compiler_error` con il nome del modello, in ogni target (non solo prod, su scelta esplicita dell'utente — scartata l'alternativa "solo su target prod" proposta come opzione più morbida). Non ancora applicata upstream.

## [2026-07-28] develop | rinominata get_dt_settimana in get_dt_accettazione
Utente ha fatto notare l'incoerenza: `get_dt_osservazione` prende il nome dalla colonna che restituisce (`DT_OSSERVAZIONE`), quindi la macro analoga per `DT_ACCETTAZIONE` andava chiamata `get_dt_accettazione`, non `get_dt_settimana` (quest'ultimo nome era già stato corretto per la var interna ma non per il nome della macro/file). Rinominato `develop/macros/materialization/get_dt_settimana.sql` → `get_dt_accettazione.sql` (macro e contenuto invariati a parte il nome), aggiornati tutti i richiami: `delete_week.sql` (default `date_expr=get_dt_accettazione()`) e `develop/models/L1/POSTE/mailc_esiti_tgb.yml` (pre_hook). `delete_week` come nome resta corretto (descrive la cadenza, come `delete_month`, non la colonna).

## [2026-07-28] develop | MAILC_ESITI_TGB (POSTE): riclassificato da cluster C a cluster D con delete_week
Utente ha chiesto di "fixare" `raw/dwh-code/models/L1/POSTE/stg_mailc_esiti_tgb.yml` con la logica di `delete_week`/`get_dt_settimana` appena creata. Verificato lo stato attuale: modello ephemeral cluster C che alimenta uno snapshot SCD2 (`snapshots/L1/POSTE/mailc_esiti_tgb.yml`, unique_key `id_postalizzazione`) — un ephemeral non è materializzato come tabella fisica, quindi non può avere pre_hook `DELETE`; la logica `delete_week` si applica solo a modelli incrementali `append` (cluster D). Chiesto chiarimento all'utente: confermato che l'intento è riclassificare l'archivio da cluster C a cluster D (ha semantica di full settimanale su `DT_ACCETTAZIONE`, non di storia SCD2 per chiave). Proposto in `develop/models/L1/POSTE/`: `mailc_esiti_tgb.yml`/`.sql` (rinominati da `stg_mailc_esiti_tgb`, drop prefisso `stg_` non più pertinente per cluster D), `materialized: incremental`/`incremental_strategy: append`, `pre_hook: delete_week('DT_ACCETTAZIONE', get_dt_settimana())`, `meta.cluster: ["D"]`, constraint PK `[id_postalizzazione, dt_accettazione]` (pattern analogo a `xerox_sla.yml`, con `dt_accettazione` al posto di `dt_osservazione`). Il `.sql` è invariato nel corpo, solo rinominato. **Nota**: su indicazione dell'utente, `get_dt_settimana()` è richiamata senza passare `'ts_riferimento'` (a differenza del pattern L1 di `get_dt_osservazione('ts_riferimento')`) — usa quindi la `var('dt_settimana')` da CLI o il default a oggi/ultimo venerdì, non il timestamp per riga. **Da applicare upstream**: oltre a portare i due nuovi file, va rimosso `snapshots/L1/POSTE/mailc_esiti_tgb.yml` e i vecchi `stg_mailc_esiti_tgb.yml`/`.sql`, non toccati da questa proposta (mai editati file in `raw/`).

## [2026-07-28] develop | nuove macro get_dt_settimana/delete_week (analoghe a get_dt_osservazione/delete_month, su base settimanale)
Richiesta utente: una `delete_week` analoga a `delete_month` ma su `DT_ACCETTAZIONE` invece di `DT_OSSERVAZIONE`, relativa al venerdì precedente invece che alla fine del mese precedente. Create in `develop/macros/materialization/`: `get_dt_settimana(ts_riferimento=none)` (mirror di `get_dt_osservazione`: con `ts_riferimento` resta sullo stesso giorno se è già venerdì altrimenti torna al venerdì precedente via `DATEADD(DAY, -MOD(DAYOFWEEKISO(...)-5+7,7), ...)`; senza, usa `var('dt_accettazione')` o default a oggi/venerdì precedente — nome var allineato alla colonna target, come `dt_osservazione` per `get_dt_osservazione`, corretto dopo segnalazione utente) e `delete_week(column='DT_ACCETTAZIONE', date_expr=get_dt_settimana())` (stesso pre-hook pattern di `delete_month`, ma usa `{{ column }}` nel WHERE fin da subito — non riproduce il bug di `delete_month` corretto in [[log]] 2026-07-27). Entrambe proposte, non esistono equivalenti in `raw/dwh-code/`.

## [2026-07-28] query | proposta job L1: da paragrafo discorsivo a elenco utility
Su richiesta dell'utente, [[proposta-testo-job-l1]] è stata resa più concisa e concreta: sostituito il paragrafo discorsivo con un elenco puntato delle utility coinvolte (`generate_jobs.ps1`, `dbt-jobs-as-code`, `fetch_dbt_jobs.py`/`fetch_dbt_dependencies.py`), una riga ciascuna, senza scendere nei comandi — il dettaglio operativo resta nella guida sviluppo 4.2, non toccata.

## [2026-07-28] query | aggiornata proposta generazione modelli L1 con TECH.CFG_L0_L1_MODULO_LOOKUP
Utente ha risincronizzato `raw/dwh-code/` e segnalato che la generazione modelli usa un'ulteriore tabella di configurazione. Verificato in `generate_source.sql`/`generate_yaml.sql`: per gli archivi OCS le macro joinano ora anche `TECH.CFG_L0_L1_MODULO_LOOKUP` (`mlk.cd_modulo = s.cd_modulo`) e generano una struttura a due livelli `OCS/<cd_modulo_l1>/<cd_modulo>` — byte-identico alla proposta `develop/` del 2026-07-24, quindi applicato upstream (non più una proposta separata). Aggiornati [[proposta-testo-generazione-modelli-l1]] (macro, esempio output, "Basi di questa proposta") e la nota di staleness in [[macro-catalogo-dbt]] (diceva ancora "non ancora portata upstream"). Aggiunta nota esplicita in [[proposta-testo-generazione-modelli-l1]]: la logica di raggruppamento `cd_modulo`/`cd_modulo_l1` (es. split `ANA`/`XAN`) è già spiegata più sopra nel capitolo L0 di `caricamento_l0_l1.docx` (struttura moduli/archivi su S3), che andrà rivisto per restare coerente — non fatto in questo giro, segnalato come lavoro futuro.

## [2026-07-27] query | proposte di correzione testo per raw/Agos X - Caricamento layer L0-L1.docx
Su richiesta utente, redatte due proposte di testo per correggere l'ultima sezione (capitoli log e test) del docx, basate su [[inconsistenze]] voci 6 e 7 e sui file `develop/`: (1) [[proposta-testo-log-l1]] sostituisce il capitolo "L1: Raccolta dei log" (marcato obsoleto nel documento stesso, basato su `dbt_artifacts` mai presente nel codice) con la descrizione di `log_run_results`/`TECH.LOG_DBT`/`LOGS.EVENT_LOG`/viste `V_EVENT_LOG`/`V_LAST_RUN_STATUS`; (2) [[proposta-testo-test-l1-ocs]] corregge i nomi dei test generici custom (`primary_key_table`/`try_cast_table` → `primary_key`/`try_cast`), limitata al processo OCS su richiesta esplicita dell'utente. `raw/` resta immutabile: le pagine sono testo da incollare a mano nel `.docx`, non un'edit del file stesso.

## [2026-07-27] query | proposta completamento paragrafo troncato "L1: Generazione dei modelli DBT"
Utente ha segnalato che il paragrafo su `transcod_dtype`/nuovo archivio vs evolutiva/OCS vs non-OCS si interrompe a "Nel dettaglio..." senza continuare. Verificato contro `raw/dwh-code/macros/generate_models/transcod_dtype.sql` (mappatura tipi, sentinella `TRANSCOD_ERROR`) e `raw/dwh-code/generate_models.ps1` (skip-on-exists salvo `-Force`, struttura cartelle `OCS/<MODULO>` vs `<SORGENTE>` piatta, escape nome riservato `CON`). Filata in [[proposta-testo-generazione-modelli-l1]].
Su richiesta successiva dell'utente, la proposta è stata estesa per essere **autoconclusiva** nel capitolo di `caricamento_l0_l1`, integrando la parte CLI/`--force`/esempio della sezione 4.1 "Generazione Modelli" di `guida_sviluppo.docx` (così la 4.1 può essere rimossa da lì). Testo mantenuto conciso nello stile di `caricamento_l0_l1`. Rimossa dalla tabella CLI l'opzione per generare tutti i modelli senza filtri (comando rischioso, non da mettere in evidenza). Aggiunto poi un breve capitolo discorsivo, non tecnico, sulla gestione dei job dbt Cloud — su ulteriore richiesta dell'utente, filato in una pagina query separata, [[proposta-testo-job-l1]] (non più dentro [[proposta-testo-generazione-modelli-l1]]). La 4.2 "Gestione Job dbt Cloud" di `guida_sviluppo` resta il riferimento per il dettaglio operativo e non viene toccata.

## [2026-07-27] develop | custom_to_date: fix variabile non definita col_str
Il messaggio di errore in `raise_compiler_error` referenziava `col_str`, mai definita nella macro (probabile residuo di refactor) — causava `UndefinedError` di Jinja invece del messaggio d'errore voluto ogni volta che la colonna aveva length 5. Corretto usando `column` (già in scope come parametro della macro). Proposto in `develop/macros/dtype_conversion/custom_to_date.sql`, non ancora portato upstream.

## [2026-07-27] develop | delete_month: parametro column ignorato nella DELETE
La macro accetta un parametro `column` (default `DT_OSSERVAZIONE`) ma la clausola `WHERE` era hardcoded su `DT_OSSERVAZIONE`, ignorando qualsiasi valore passato dal chiamante — il parametro era quindi inutile/ingannevole per chi lo invoca con una colonna diversa. Corretto usando `{{ column }}` nel WHERE. Proposto in `develop/macros/materialization/delete_month.sql`, non ancora portato upstream.

## [2026-07-24] develop | fix chiave di join invertita in CFG_L0_L1_MODULO_LOOKUP — bug già live upstream
Prima correzione (parziale, poi rivista): utente ha segnalato l'ordine soprafolder/sottofolder invertito nel path, corretto invertendo la concatenazione ma lasciando il join `mlk.cd_modulo_l1 = s.cd_modulo` invariato.
Chiarimento successivo dell'utente con dati concreti: in `CFG_L1_SCHEMA.cd_modulo` si osservano i valori `ANA` e `XAN`; nella lookup `CFG_L0_L1_MODULO_LOOKUP`, la colonna **`cd_modulo`** vale `ANA`/`XAN` (stessa chiave di `CFG_L1_SCHEMA.cd_modulo`) mentre **`cd_modulo_l1`** vale `ANA` per entrambe le righe (il valore aggregato/soprafolder). Questo significa che il join andava fatto su **`mlk.cd_modulo = s.cd_modulo`**, non su `mlk.cd_modulo_l1 = s.cd_modulo` come avevo scritto — avevo scambiato le due colonne della lookup. Struttura di cartelle reale attesa: `OCS/ANA/{ANA,XAN}` (soprafolder = `cd_modulo_l1` dalla lookup, comune; sottofolder = `cd_modulo` grezzo, quello che distingue le due entry).
**Nota importante**: la versione con il join sbagliato era stata portata upstream nel resync del 2026-07-24 (era byte-identica alla nostra proposta) — il bug è quindi già live in `raw/dwh-code/`.
Corretto in tutte e 4 le macro in `develop/macros/generate_models/`: join ora su `mlk.cd_modulo = s.cd_modulo`, select `mlk.cd_modulo_l1` (rinominate le variabili jinja da `modulo_l1`/`modulo_l0` a `modulo_raw`/`modulo_l1` per riflettere la semantica corretta), `modulo_path = modulo_l1 ~ '/' ~ modulo_raw`. In `generate_yaml.sql` anche `schema_tag` (usato nel `query_tag`) aggiornato per usare `modulo_raw` (comportamento invariato rispetto a prima, era già basato sul valore grezzo). `generate_models.ps1`/`generate_jobs.ps1` non richiedono modifiche. Da portare a monte con priorità, visto che la versione sbagliata è già deployata.

## [2026-07-24] develop | generate_models.ps1: fix escape CON_ per path OCS a due livelli
Utente ha notato che il nostro path OCS a due livelli (`cd_modulo_l1/cd_modulo`) rompe l'escape del nome riservato Windows `CON`: `Resolve-Mod` in `Get-OutputDir` fa match esatto (`$mod -eq 'con'`) sull'intera stringa `$modulo`, che ora può essere `"con/xyz"` — non più uguale a `'con'`, quindi l'escape non scatta più sul segmento e si rischia di creare una cartella letteralmente chiamata `CON` (nome riservato, invalido su Windows). Creato per la prima volta `develop/generate_models.ps1` (finora non esisteva in develop, solo le 4 macro erano state proposte): `Get-OutputDir` ora fa `$modulo -split '/'`, applica `Resolve-Mod`+`ToUpper()` a ciascun segmento, poi rijoina con `/`. Nessun'altra riga toccata rispetto a `raw/dwh-code/generate_models.ps1`. Non ancora portato upstream.

## [2026-07-24] lint | re-verifica completa dopo resync raw/dwh-code (commit "new fetch")
Utente ha risincronizzato `raw/dwh-code/` da GitLab (commit `55db8b0`, ~100 file). Lint completo via agente in background: (1) **query_tag** — 73 degli 85 fix proposti in `develop/` il 2026-07-22 sono stati portati upstream (tutta l'area L2 tranne CARTE). Restano aperti CARTE (6 file, schema errato) e L3 `basilea_core`/`monitoraggio_produzione` (9 file, assente) + la nuova area L3 **CAMPIONI** (3 file, mai avuta query_tag, non coperta dal fix del 2026-07-22 perché non esisteva ancora) — aggiornati `queries/inconsistenze.md` voce 1, `concepts/query-tag-monitoring.md`, `index.md`. (2) **generate_models/generate_jobs** — la proposta `develop/` (join `CFG_L0_L1_MODULO_LOOKUP`, path OCS a due livelli, modulo=sottofolder) risulta **byte-identica** nel codice risincronizzato: applicata upstream, rimossa da `index.md` Develop. (3) **variazioni_anagrafiche** — il rename `PROGRESSIVO_CONTROPARTE`→`PR_CONTROPARTE` è stato applicato upstream (risolve il residuo di [[inconsistenze]] voce 4), ma la struttura con lo split ramo1/ramo2 (bug segnalato 2026-07-22, non funzionante) è ancora quella live — il nostro fix in `develop/` non è stato portato e resta da applicare, aggiornato `entities/l2-anagr-controparte.md`. (4) **Gap nuovi non documentati**: sorgente POSTE (`mailc_esiti_tgb`), sorgente SFC (`documentirichiesti__c`, `praticacase__c`, `user`, modifiche a `casehistory`), area L3 CAMPIONI — nessuna pagina wiki li menziona, segnalati in `queries/inconsistenze.md` come gap da ingerire (non creata alcuna pagina). (5) risolti anche di riflesso: bug naming `ristrutturazioni_o_sql`/`_yml` (ora con estensione corretta), file orfano `variazioni_anagrafiche.sql.old` (rimosso). Concept page `concepts/storicizzazione-l1-cluster-a-b-c.md`, `concepts/naming-convention-agos-x.md`, `concepts/cancellazioni-fl-deleted.md` non riverificate riga per riga in questo giro (aree non toccate dal resync) — segnalato come non conclusivo in `queries/inconsistenze.md`.

## [2026-07-24] develop | plan-jobs.ps1: output di dbt-jobs-as-code plan su file di default
Utente ha chiesto che `plan-jobs.ps1` scriva l'output di `dbt-jobs-as-code.exe plan` su un file di default, non solo a schermo. Aggiunta pipe a `Tee-Object -FilePath plan_output.txt` (stampa comunque a console e in più salva su file). Scritto in `develop/plan-jobs.ps1`, riallineato anche ai path corretti di `raw/dwh-code/plan-jobs.ps1` (il file develop preesistente aveva refusi non correlati: `.models\` invece di `.\models\`, `--ouput-file` invece di `--output-file`). Non ancora portato upstream in `raw/dwh-code/` né nella repo live `dwh-x-dbt`.

## [2026-07-24] develop | plan-jobs.ps1: fix UnicodeEncodeError (charmap, ❌) introdotto dal Tee-Object
Dopo il redirect dell'output di `dbt-jobs-as-code.exe plan` su file (modifica precedente), l'utente ha riportato l'errore Python `'charmap' codec can't encode character '❌'`. Causa: `dbt-jobs-as-code.exe` è un tool Python che, quando lo stdout non è una console reale ma viene reindirizzato/piped (come con `Tee-Object`), Python smette di rilevare UTF-8 e usa la codepage di sistema (cp1252 su Windows IT) per encodare l'output — che non sa rappresentare l'emoji ❌ usata dal tool nel diff. Fix: impostato `$env:PYTHONIOENCODING = 'utf-8'` prima della chiamata al tool in `develop/plan-jobs.ps1`. Non ancora portato upstream in `raw/dwh-code/` né nella repo live `dwh-x-dbt`.

## [2026-07-24] develop | plan-jobs.ps1: fix mojibake sui caratteri accentati dopo il fix PYTHONIOENCODING
Dopo aver forzato `PYTHONIOENCODING=utf-8` (fix precedente), l'utente ha segnalato output illeggibile in console (sequenze tipo "Ooe" ripetute al posto delle vocali accentate/box-drawing). Causa: il tool ora scrive byte UTF-8 corretti, ma la console PowerShell li interpretava ancora con la codepage di sistema (cp1252/850), producendo mojibake. Fix in `develop/plan-jobs.ps1`: aggiunto `[Console]::OutputEncoding = [System.Text.Encoding]::UTF8` prima della chiamata. Tentativo iniziale con `Tee-Object -FilePath plan_output.txt -Encoding utf8`: **errore, `Tee-Object` in Windows PowerShell 5.1 non ha il parametro `-Encoding`** (esiste solo da PS7 in poi). Corretto catturando l'output in `$planOutput`, scritto su file con `Out-File -FilePath plan_output.txt -Encoding utf8` e poi stampato a console con `$planOutput`, così sia lo schermo (grazie a `[Console]::OutputEncoding`) che il file sono coerenti in UTF-8. Non ancora portato upstream in `raw/dwh-code/` né nella repo live `dwh-x-dbt`.

## [2026-07-24] develop | generate_source: primary_key anche per OCS, non più unique_key
`generate_source.sql` distingueva il test data quality sulla PK in base a `is_ocs`: `unique_key` per OCS, `primary_key` per le altre sorgenti. Utente ha chiesto di usare `primary_key` anche per OCS. Rimosso il branch `if/else` su `is_ocs`, ora il macro emette sempre `primary_key` quando `pk_cols` non è vuoto, indipendentemente dalla sorgente. Aggiornato `develop/macros/generate_models/generate_source.sql` (già in develop per la modifica precedente sul join `CFG_L0_L1_MODULO_LOOKUP`). Non ancora portato upstream in `raw/dwh-code/` né nella repo live `dwh-x-dbt`.

## [2026-07-24] develop | generate_jobs.ps1: modulo = solo sottofolder, non più soprafolder; jobKey resta su category
Con la nuova struttura a due livelli `OCS/{soprafolder}/{sottofolder}` sotto `models/L1`, `generate_jobs.ps1` (raw) calcola `category` = `pathParts[levelIndex+2]` (la soprafolder) e la usa come `modulo` nella description del job, ignorando del tutto il terzo livello di path. Utente ha chiesto che `modulo` contenga la sottofolder, non la soprafolder. Aggiunta cattura di `subCategory` = `pathParts[levelIndex+3]` e `moduloVal` ora usa solo `subCategory` (era `category`). Avevo inizialmente aggiunto `subCategory` anche al `jobKey`, ma l'utente ha chiarito che il nome del job deve restare identico per tutte le sottofolder di una stessa soprafolder (es. `AGOS_L1_O_ANA_<modello>` sia per `ANA/XAN/<modello>` che per `ANA/ANA/<modello>`) — reso il `jobKey` di nuovo basato solo su `category`, come in origine. **Nota**: questo significa che due modelli con lo stesso nome in sottofolder diverse della stessa soprafolder produrranno lo stesso `jobKey` e si sovrascriveranno a vicenda nell'hashtable `$jobs` — comportamento accettato esplicitamente dall'utente, da tenere presente se emerge un caso reale. `fetch_dbt_jobs.py` non modificato: legge `modulo` così com'è dalla description, non contiene logica propria da correggere. Scritto in `develop/generate_jobs.ps1`. Non ancora portato upstream in `raw/dwh-code/` né nella repo live `dwh-x-dbt`.

## [2026-07-24] develop | join con TECH.CFG_L0_L1_MODULO_LOOKUP in generate_source/yaml/model/snapshots (path OCS a due livelli)
Utente ha chiesto di aggiungere, solo per le tabelle `ds_sorgente = 'OCS'`, una `left join` a una nuova tabella tecnica `TECH.CFG_L0_L1_MODULO_LOOKUP` (CD_MODULO, CD_MODULO_L1, DS_SORGENTE) sulla chiave `cd_modulo_l1` (= `s.cd_modulo` in `CFG_L1_SCHEMA`), per creare i modelli generati in una sottocartella aggiuntiva `cd_modulo_l1/cd_modulo` invece del solo `cd_modulo_l1` attuale. Modificate le 4 macro in `raw/dwh-code/macros/generate_models/` (`generate_source`, `generate_yaml`, `generate_model`, `generate_snapshots`): aggiunta la join (condizionata a `upper(s.ds_sorgente) = 'OCS'`), e la riga di annotazione `### modulo:` ora emette `<cd_modulo_l1>/<cd_modulo_l0>` quando la sorgente è OCS e il lookup produce un match, altrimenti resta `<cd_modulo_l1>` come prima. `generate_models.ps1` (root repo) **non modificato**: `Get-OutputDir`/`New-Item -Force -Path` creano già le sottocartelle annidate se il valore parsato da `### modulo:` contiene uno slash, quindi il parsing testuale esistente funziona invariato. Scritto in `develop/macros/generate_models/` (le 4 macro). Non ancora portato upstream in `raw/dwh-code/` né nella repo live `dwh-x-dbt`.

## [2026-07-22] develop | corretto fix variazioni_anagrafiche: window function annidate, SELECT *, naming PR_PK/PR_CONTROPARTE
Utente ha segnalato 3 problemi nella prima stesura del fix precedente (stesso giorno): (1) `ts_inizio_expr` (CASE con ROW_NUMBER) non può essere passato come argomento a `ts_fine_validita()` — genera window function annidate, non ammesse da Snowflake; risolto con CTE separate `BASE_RAW`→`BASE`. (2) mai `SELECT *`, sempre lista esplicita — sostituiti i due `SELECT *, ...` in `BASE` e `DEDUP_FV`. (3) naming: `PROGRESSIVO_PK`/`PROGRESSIVO_CONTROPARTE` (nomi legacy portati dal file `.old`) rinominati a `PR_PK`/`PR_CONTROPARTE` per allinearsi alla convenzione corrente; aggiornato anche il develop yml corrispondente. Vedi [[l2-anagr-controparte]] per il dettaglio tecnico.

## [2026-07-22] develop | ripristinata struttura .old di variazioni_anagrafiche con logica DT_INSERIMENTO integrata
Utente ha segnalato che la versione con split ramo1/ramo2 (fix del 2026-07-21, `develop/models/L2/ANAGR_CONTROPARTE/variazioni_anagrafiche.sql`) non funziona. Recuperata `raw/dwh-code/models/L2/ANAGR_CONTROPARTE/variazioni_anagrafiche.sql.old` (struttura `BASE`→`DEDUP` con macro condivisa `is_incremental_S1`, nessuno split) e reintegrata la logica "primo record storico usa AL_DATA_INSERIMENTO". Poiché in questa struttura `BASE` rilegge sempre l'intero storico ad ogni run (il filtro incrementale è applicato dopo, dentro `is_incremental_S1`), `ROW_NUMBER()=1` per controparte individua già il vero primo record storico senza bisogno del check `EX.CD_CONTROPARTE IS NULL` usato nella versione con split (necessario lì solo perché il ramo1 leggeva solo il delta). Anche `TS_FINE_VALIDITA` di `BASE` aggiornato per usare la stessa espressione CASE di `TS_INIZIO_VALIDITA` (coerenza del `LEAD` in `ts_fine_validita()`). Scritto in `develop/models/L2/ANAGR_CONTROPARTE/variazioni_anagrafiche.sql` (sovrascritto), sostituendo il fix precedente. Aggiornate [[l2-anagr-controparte]] e `index.md` (sezione Develop). Non ancora portato upstream in `raw/dwh-code/` né nella repo live `dwh-x-dbt`.

## [2026-07-22] develop | fix query_tag mancante/errato in 85 file yml L2/L3
Scansionati tutti i 107 file yml modello sotto `raw/dwh-code/models/L2/` e `models/L3/` (esclusi L1). 85 avevano un `query_tag` mancante o errato: 63 completamente assenti (ANAGR_CONTROPARTE, ANTIFRODE, ASSICURAZIONI, GESTIONE_CREDITI, ONBOARDING, PRODOTTO, PRODOTTO_M, SWORD, L3 basilea_core/monitoraggio_produzione), 21 con schema errato (CARTE: `L2_PRODOTTO` invece di `L2_CARTE`, 6 file; SCORE_BANCHE_DATI: `L2_SCORING` invece di `L2_SCORE_BANCHE_DATI`, 15 file), 1 con entita errata (`indice_rischio_m.yml`: `INDICE_RISCHIO` invece di `INDICE_RISCHIO_M`). Scritti 85 file corretti sotto `develop/models/L2/` e `develop/models/L3/`, mirror esatto della struttura raw, con solo la chiave `query_tag` toccata in ciascun `config:`. Aggiornate [[inconsistenze]] (voce 1 marcata risolta-in-develop) e [[query-tag-monitoring]] (copertura reale riverificata). La modifica vive solo in `develop/` — non ancora portata a monte in `raw/dwh-code/` né nella repo live `dwh-x-dbt`; l'utente deve applicarla manualmente.

## [2026-07-21] develop | fix logica "primo record" in variazioni_anagrafiche (ROW_NUMBER su delta, non su storia completa)
Utente ha notato che il `ROW_NUMBER()` righe 9-16 di `raw/dwh-code/models/L2/ANAGR_CONTROPARTE/variazioni_anagrafiche.sql`, usato per assegnare `AL_DATA_INSERIMENTO` come `TS_INIZIO_VALIDITA` al "primo record storico" di ogni controparte, è partizionato solo sulle righe del batch incrementale corrente (`CC`, già filtrato da `LASTMODIFIEDDATA >`), non su tutta la storia in `ccanalog`: per una controparte già esistente in target, la riga più vecchia del delta corrente viene scambiata per il vero primo record. Scritto fix in `develop/models/L2/ANAGR_CONTROPARTE/variazioni_anagrafiche.sql`: aggiunta `AND NOT EXISTS (SELECT 1 FROM {{ this }} WHERE CD_CONTROPARTE = CC.AL_CODICE)` (solo sotto `is_incremental()`) alla condizione del CASE, così la logica si applica solo a controparti realmente nuove per il target. Aggiornata [[l2-anagr-controparte]] con nota sul nuovo bug e sul fix proposto. Non ancora portato upstream nella repo live.

## [2026-07-20] lint | chiarito standard cancellazioni L2 in inconsistenze
Utente ha confermato che lo standard per le cancellazioni L2 è il meccanismo con `pre_hook delete_l2(...)`, non un'ambiguità tra due pattern equivalenti. Aggiornata voce 2 di [[inconsistenze]]: il codice che usa solo `WHERE FL_DELETED='N'` senza pre_hook è classificato come disallineato rispetto allo standard, non come "meccanismo alternativo non chiaro".

## [2026-07-20] ingest | logica L1 di normalizzazione placeholder OCS trovata nel codice
Utente sosteneva che esista logica L1 (IF) che tratta i placeholder OCS ' ' vs NULL, non solo una nota di framework. Verificato nel codice reale: i 14 modelli L1 `raw/dwh-code/models/L1/OCS/AIN/*.sql` applicano sistematicamente `TRY_CAST(IFF(RTRIM(campo)='', ' ', RTRIM(campo)) AS ...)` per ogni campo varchar — logica L1 vera, non generata dal template (`templates/models/L1/C/stg_table.sql` non ce l'ha), scritta a mano solo per OCS/AIN. Direzione opposta a quanto discusso finora: L1 *produce/canonicalizza* il placeholder ' ' (mai '' vuota), non lo converte in NULL. Aggiunta sezione "Normalizzazione varchar vuoti OCS in L1" in [[storicizzazione-l1-cluster-a-b-c]] con citazioni a file/righe reali, e collegamento incrociato con [[null-vs-placeholder-ocs]].

## [2026-07-20] ingest | risync raw/dwh-code + guida sviluppo aggiornata
Utente ha risincronizzato `raw/dwh-code/` e aggiornato `raw/guida_sviluppo.docx`. Aggiornata [[guida-sviluppo]]: nuova sezione 5.5 "Gestione campi varchar vuoti OCS" (delegata a [[null-vs-placeholder-ocs]], task separato); rinomina `PROGRESSIVO_PK`→`PR_PK` nella sezione 5.1 (variante S1 senza PK propria), verificata anche nel codice (`variazioni_anagrafiche.sql`/`.yml`) e propagata a [[l2-anagr-controparte]], [[progressivo-pk-e-progressivo-controparte]], [[naming-convention-agos-x]]. Spot-check di [[l2-antifrode]], [[l2-carte]], [[l2-onboarding]], [[l2-provvigioni-rappel]], [[macro-catalogo-dbt]], [[storicizzazione-l1-cluster-a-b-c]], [[storicizzazione-l2-s1-s4]] contro lo stato corrente di `raw/dwh-code/`: già coerenti, nessuna modifica necessaria oltre al rename PR_PK. Riallineata [[inconsistenze]] alla richiesta esplicita dell'utente: rimosso lo storico delle voci risolte (restano solo le 10 incongruenze attualmente aperte), voce 4 (prefissi) ridotta al solo `PROGRESSIVO_CONTROPARTE` residuo, aggiunta sezione "Verifiche eseguite" concisa senza log storico.

## [2026-07-17] query | riscritta null-vs-placeholder-ocs secondo guida sviluppo ufficiale
Utente ha condiviso le linee guida ufficiali (guida sviluppo, "Impatti su L2"): sostituire IS NULL su campi OCS con macro `custom_is_null('campo')` (copre `IS NULL OR = ' '`), integrare i COALESCE con primo input OCS con `NULLIF(campo, ' ')`, salvo eccezioni note (unico caso: `BACCPTES`, dove `' '` = "Poste Italiane" — verificato che non è referenziato in nessun modello L2/L3 vendorizzato, nessuna eccezione attiva). JOIN/UNION dichiarati tema separato ancora in discussione interna, non scansionati. Cancellata e riscritta [[null-vs-placeholder-ocs]] come inventario azionabile in due tabelle (IS NULL da migrare a `custom_is_null()`, COALESCE da integrare con `NULLIF`), riusando i risultati dello scan completo del giro precedente ma riclassificati secondo le nuove regole — in particolare `passaggi_a_perdita.sql:8,32` passa da "non è bug" a "da correggere" perché il criterio ora è la sorgente del primo input COALESCE, non solo l'origine del NULL osservato.

## [2026-07-17] query | rifatto scan completo NULL-vs-placeholder OCS su tutto L2+L3
Su richiesta utente, rifatto da zero lo scan (sostituendo interamente la pagina precedente) su TUTTI i modelli L2 e L3 vendorizzati, scope ristretto a `IS NULL`/`IS NOT NULL`/`COALESCE`/`NVL`/`IFNULL`/`ZEROIFNULL` su colonne stringa (esclusi concatenazioni, MIN/MAX, LISTAGG, ORDER BY NULLS, cast impliciti, come richiesto). 6 sub-agent in parallelo, uno per gruppo di moduli. Risultato: 4 bug confermati ad alta severità (`svalutazioni_m.sql:47`, `flessibilita_m.sql:51`, `perdite_minime_abb.sql:43`, `carta.sql:73-77`/`carta_m.sql:70-71`), 5 candidati non determinabili per provenienza/tipo colonna incerto (`carte_utilizzi.sql:143,191`, `doc_istruttoria.sql:19-26`, `pratica.sql:149,570`). Declassati 2 finding del giro precedente dopo verifica più approfondita: `moratorie_m.sql:56,68` (il NULL viene da una LEFT JOIN senza match, non dal placeholder sorgente — pattern legittimo) e `carte_limitazioni_operativita.sql:12` (ramo IS NULL morto ma innocuo, `<>'FF'` copre già il placeholder). Confermato nessun bug in SWORD/SCORE_BANCHE_DATI (leggono da XML NO-OCS) e riconfermato il ritiro del finding su `dm_controlli_basilea_m.sql` (sorgente `L1_O_BAS` esterna, non OCS). Corretta anche una classificazione errata di un sub-agent (`svalutazioni_m.sql:47` segnalato come "positivo" ma in realtà bug: `' ' &lt;&gt; ''` è vero, il placeholder passa comunque i due controlli). Riscritta interamente [[null-vs-placeholder-ocs]].

## [2026-07-17] query | ritirato finding Basilea da null-vs-placeholder-ocs (sorgente non OCS)
Su domanda dell'utente ("non c'era roba su Basilea?"), riverificata la provenienza di `dm_controlli_basilea_m.sql`. Scoperto che il modello non usa `ref()`/`source()` ma legge con `FROM` hardcoded da `{{ env_var('DBT_DATABASE') }}.L1_O_BAS.IFBLFSCRCO_TEST`/`IFBLFSCRCA_TEST` — schema/tabelle assenti sia in `raw/dwh-code/models/L1/` sia in `TECH.CFG_L1_SCHEMA`/`CFG_L1_CLUSTER_STO` appena ingeriti. Non fa parte della pipeline L0→L1 OCS standard: ritirato il finding "alta severità" su `C_CDOCC`/`CD_CLUSTER_CCF`/ecc. in [[null-vs-placeholder-ocs]] perché non verificabile come OCS (probabile output di un motore di calcolo Basilea esterno con convenzione ignota per i valori assenti).

## [2026-07-17] ingest | CFG_L1_SCHEMA e CFG_L1_CLUSTER_STO (export CSV)
Utente ha fornito `raw/cfg_l1_schema.csv` (139.495 righe, catalogo colonne per 1.737 archivi/50 moduli, export di `TECH.CFG_L1_SCHEMA`) e `raw/cfg_l1_cluster_sto.csv` (1.737 righe, mappatura archivio→cluster→sorgente, export di `TECH.CFG_L1_CLUSTER_STO`) — entrambe già citate come tabelle tecniche in [[caricamento-layer-l0-l1]]. Non ingeriti riga per riga (volume), solo struttura e statistiche in [[cfg-l1-schema-e-cluster-sto]]. Trovate due conferme importanti: (1) OCS è la sorgente di 1.613/1.737 archivi (92.9%) — conferma quantitativa autoritativa di quanto discusso con l'utente in [[null-vs-placeholder-ocs]] (tutti i 36 `CD_MODULO` non vuoti sono OCS, sostituisce l'euristica precedente "nessuna cartella L1 dedicata = OCS"); (2) il cluster B ha sotto-varianti **B1/B2** non documentate, ed esiste uno stato **TBD** (55 archivi non ancora classificati, nessuno OCS) — aggiornato [[storicizzazione-l1-cluster-a-b-c]].

## [2026-07-17] query | logiche NULL-assuming su placeholder OCS (' ') in L2/L3
Verifica su richiesta utente: le sorgenti OCS (non solo il modulo AIN vendorizzato — praticamente qualunque tabella priva di cartella L1 dedicata, prefissi CC/CR/PL/QPR/BA/OX/SV/AI/WF) non inviano mai NULL sui campi stringa, usano il placeholder `' '`. Scansionati tutti i modelli L2 (ANAGR_CONTROPARTE, ASSICURAZIONI, ANTIFRODE, PRODOTTO, PRODOTTO_M, PROVVIGIONI_RAPPEL, RISCHI_ADEMPIMENTI, SALDI, CARTE, GESTIONE_CREDITI, ONBOARDING) e L3 (monitoraggio_produzione, basilea_core) per pattern `IS NULL`/`IS NOT NULL`/`COALESCE` su colonne stringa che assumono semantica NULL standard invece del placeholder. Trovati 8 bug confermati (severità alta: `legame_ditte_individuali.sql`, `svalutazioni_m.sql`, `perdite_minime_abb.sql`, famiglia controlli Basilea in `dm_controlli_basilea_m.sql`; media: `moratorie_m.sql`, `flessibilita_m.sql`, `carte_limitazioni_operativita.sql`; bassa: `carta.sql`/`carta_m.sql`), oltre a pattern corretti già presenti come riferimento (`provvigioni_assicurative.sql`, `carte_blocchi.sql`, famiglia `wfl_*`). Analisi limitata a `IS NULL`/`IS NOT NULL`/`COALESCE`/`NULLIF` su singola colonna, join fuori perimetro. Filata in [[null-vs-placeholder-ocs]]. Mancano ancora da scansionare SWORD e SCORE_BANCHE_DATI.

## [2026-07-16] lint
Lint completo dopo il resync di `raw/dwh-code/` (utente ha rigenerato lo snapshot dalla repo live). Verificati orfani (nessuno: ogni pagina in `index.md` ha almeno un inbound `[[link]]`, controllato via grep su tutte le cartelle), contraddizioni (nessuna nuova), concetti ricorrenti senza pagina propria (nessuno nuovo oltre a query-tag-monitoring/lastmodifieddata già creati il 2026-07-15), cross-reference mancanti (nessuna nuova rilevata). Trovata **una staleness reale**: l'adozione della macro condivisa `is_incremental_S1` nei modelli S1 è molto più ampia di quanto documentato — ora usata (con `hash_cols()` a monte) in `indirizzi_postalizzazione.sql`, `carte_utilizzi.sql`, `wfl_istanza.sql`, `wfl_attivita.sql`, `wfl_fase.sql`, `wfl_sottofase.sql`, `tabelle_finanziarie.sql`, `variazioni_stato_prat.sql`, non solo in `wfl_istanza` come riportato prima; l'unico modello S1 rimasto completamente bespoke è `variazioni_anagrafiche.sql`. Aggiornati [[storicizzazione-l2-s1-s4]], [[l2-onboarding]], [[l2-anagr-controparte]], `index.md` (riga L2 ONBOARDING) e voce 9 di [[inconsistenze]] (ridotta a gap di sola documentazione, non più incongruenza funzionale). Verificato anche che il fix proposto su `variazioni_anagrafiche.sql` (TS_INIZIO_VALIDITA da AL_DATA_INSERIMENTO per il primo record) è stato applicato upstream — coerente con la pulizia di `develop/` già registrata il 2026-07-15. Riconfermate senza differenze le voci 1-8, 10, 11 di [[inconsistenze]] (query_tag CARTE/PROVVIGIONI_RAPPEL, cancellazioni ANTIFRODE, assenza `dbt_artifacts`/`remove_datamask`, nomi test, sentinelle TIMESTAMP/DATE, bug minori in `custom_to_date`/`delete_month`/`call_proc_report_fondi_masterscale`/`ristrutturazioni_o_*`): tutte confermate ancora valide, dettaglio aggiunto in [[inconsistenze]] sotto "Verifiche eseguite". Aggiornate le date `updated:` e le note di staleness in [[storicizzazione-l2-s1-s4]], [[macro-catalogo-dbt]], [[l2-carte]], [[l2-provvigioni-rappel]], [[l2-antifrode]], [[l2-anagr-controparte]], [[l2-onboarding]], [[inconsistenze]].

## [2026-07-15] develop | recupero CONTACT_HISTORY (fix dm-reader, blocco MODULO)
Su richiesta dell'utente, riverificati tutti gli skip residui delle subject area CONTATTI/DIGITAL dopo il fix precedente. Trovato un secondo bug analogo in `dm-reader` (`detect_procedures`, ramo multiprocedura basato su header `MODULO`): quando due blocchi di colonne condividono le stesse etichette (`SORGENTE TABELLA`/`SORGENTE CAMPI`/`REGOLA TECNICA`/`CHIAVI DI AGGANCIO`) nello stesso range di un singolo blocco `MODULO` (es. `MODULO OCS` seguito da un blocco `SORGENTE TABELLA FEA L1` senza il proprio header `MODULO`), il parser sovrascriveva con l'ultimo match invece del primo. Per CONTACT_HISTORY questo faceva leggere in TAB/COL il testo delle note FK del blocco FEA invece della vera sorgente OCS (`CRM-ContactHistory (DynamoDB)`, colonne 1:1 col nome campo). Stesso fix "vince la prima occorrenza" applicato anche qui. Generata `develop/models/L2/MAIN/contact_history.sql`. Verificato invece che `ANA_CAMPAGNE_MKTG` non ha alcuna colonna `FLAG_PK` nell'header (non un bug di parsing) e resta correttamente non generabile; `ANAGRAFICA_STEP_JOURNEY`/`CATALOGO_JOURNEY` confermati senza PK/tipi per nessun campo. Aggiunta al termine della SKILL.md di `dm-reader` una sezione "Se l'output non e' quello atteso" che impone di leggere il foglio grezzo via openpyxl prima di concludere che un'entita' non e' generabile.

## [2026-07-15] develop | recupero SESSIONI e ANA_INIZIATIVE_COMMERCIALI (fix dm-reader)
Su richiesta dell'utente, ricontrollati a mano i 2 fogli inizialmente skippati come "senza sorgente utilizzabile". Trovato un bug reale in `dm-reader` (`.claude/skills/dm-reader/scripts/dm.py`, `detect_procedures`): nel fallback "procedura singola", quando l'header ripete le etichette SORGENTE TABELLA/SORGENTE CAMPI/REGOLA TECNICA/CHIAVI DI AGGANCIO piu' volte senza prefisso MODULO (blocchi doppi TIG-CO/TIG-CA su foglio a procedura unica, es. SESSIONI), il parser sovrascriveva l'indice colonna a ogni occorrenza finendo per usare l'ULTIMO blocco (vuoto) invece del primo (popolato) — TAB/COL apparivano vuoti per ogni campo. Fix: vince la prima occorrenza. SESSIONI ora si legge correttamente e generata in `develop/models/L2/FORM_E_PREVENTIVATORI/sessioni.sql`. ANA_INIZIATIVE_COMMERCIALI invece non era un bug del parser ma un errore di compilazione nello sheet stesso: colonne TAB/COL shiftate di una posizione (TAB conteneva una descrizione, COL il nome tabella reale CCTABICO, e il vero nome colonna sorgente era nella colonna REGOLA FUNZIONALE, es. TABICO_INIZIATIVA_COMM) — shift confermato sistematico su tutte le righe del foglio, ricostruita la mappatura corretta e generata in `develop/models/L2/FORM_E_PREVENTIVATORI/ana_iniziative_commerciali.sql` (3 campi rimasti NULL+WARN per sorgente/RT davvero assente).

## [2026-07-15] develop | 13 entita' L2 subject area CONTATTI e DIGITAL
Generati SQL+YML per 13 delle 19 entita' delle subject area CONTATTI e DIGITAL in `raw/Agos X - Layer L2.xlsx`, sotto `develop/models/L2/{MAIN,VOC,FORM_E_PREVENTIVATORI,TRACCIATURA_DIGITAL,AREA_RISERVATA}/`: APPUNTAMENTO, CONTATTO_NGS, PREVENTIVI, SURVEY_INPUT, SURVEY_OUTPUT, RECENSIONI, ANA_VERSIONI_FORM, FORM, ANA_CAMPAGNE_TIG, RICONOSCIMENTO, ACCESSI, ISCRITTI, ABILITAZIONI_PUSH. Skippate 6 entita' senza dati sufficienti a generare SQL valido: CONTACT_HISTORY (solo relazioni concettuali FK, nessuna colonna fisica), ANA_CAMPAGNE_MKTG (foglio requisiti pre-implementazione, niente PK/RT tecnica), ANAGRAFICA_STEP_JOURNEY e CATALOGO_JOURNEY (nessuna PK/tipo dichiarato, sorgenti circolari), ANA_INIZIATIVE_COMMERCIALI (colonne TAB/COL/RT dello sheet shiftate, nomi colonna sorgente non ricavabili), SESSIONI (nessuna sorgente per nessun campo). Molte delle entita' generate hanno gap significativi nel data model (assente su richiesta utente un approfondimento minore rispetto al normale): LASTMODIFIEDDATA assente ovunque (storicizzazione dedotta come S4 in assenza di qualunque campo tecnico, da confermare), PK APPUNTAMENTO basata su una colonna sorgente ambigua ('BusinessID/subjectID') sostituita con placeholder NULL, RT con alias interni disallineati rispetto al campo dichiarato in APPUNTAMENTO (4 campi), contraddizioni tra TAB dichiarata e tabella usata nella RT in CONTATTO_NGS/PREVENTIVI. Tutti i gap segnalati inline con `-- WARN`. Nessuno dei modelli e' stato portato in `raw/dwh-code/`.

## [2026-07-15] develop | 11 DataMart L3 sotto-processo Campioni.Accettazione Agos
Generati SQL+YML per tutti gli 11 DataMart del sotto-processo `13_Campioni.Accettazione Agos` in `raw/Agos_X_-_Layer_L3_-_DataMart_CMP_2.xlsx` (DM_CA_CAMPIONI, DM_CO_CAMPIONI, DM_CA_IMP_RATA, DM_CA_MATRIX_INAT, DM_CA_TAB_BLOCCHI, DM_CA_MATRIX_UTLZ, DM_CA_MOD_RIPAGAMENTO, DM_CA_MATRIX, DM_CA_TARGET, DM_CO_TARGET, DM_CO_MATRIX), sotto `develop/models/L3/campioni_accettazione/`. Estensione a `dm-reader` (`.claude/skills/dm-reader/scripts/dm.py`): accettata anche l'etichetta colonna `TYPE` come alias di `FORMATO` nell'header degli sheet campo, necessaria per leggere questo sotto-processo. I fogli sorgente sono segnati "da placeholder 13_05": numerose RT senza logica reale ("calcolo da definire" -> NULL), campi nome con placeholder `##` (matrici tipo RATA00..120 mai risolte in colonne concrete), diverse RT con mismatch di tipo palese (DATE mappata su NUMBER o su VARCHAR) e alcuni campi duplicati nello stesso sheet (stesso nome, descrizioni diverse) — tutti trascritti letteralmente e segnalati con `-- WARN` inline, nessuna correzione di logica. Nessuno dei modelli e' ancora stato portato in `raw/dwh-code/`: proposta solo in `develop/`.

## [2026-07-15] lint
Lint completo della wiki: nessun orfano, nessuna contraddizione tra pagine, nessuna staleness rilevata vs `raw/dwh-code/` (spot-check su l2-carte, l2-provvigioni-rappel, cancellazioni-fl-deleted, macro citate), nessuna incongruenza skill-vs-codice (develop-l2/develop-l3/dm-reader coerenti col codice vendorizzato). Tutte le 11 voci di [[inconsistenze]] confermate ancora valide (nessuna risolta). Individuati 2 concetti ricorrenti senza pagina propria: creata [[query-tag-monitoring]] (estratta da storicizzazione-l2-s1-s4, linkata da l2-carte/l2-provvigioni-rappel) e [[lastmodifieddata]] (consolidata da caricamento-layer-l2/macro-catalogo-dbt/cancellazioni-fl-deleted). Discusso e adottato il pattern di tracking incongruenze: pagina unica `queries/inconsistenze.md` (rinominata da `inconsistenze-doc-vs-codice.md`) con tabella riassuntiva a 3 colonne Codice/Skill/Doc in cima e dettaglio narrativo sotto (12 voci, inclusa la verifica skill-vs-codice); voci non cancellate ma marcate risolte con data quando superate. Aggiornati tutti i wikilink nella wiki al nuovo nome pagina.

## [2026-07-15] develop | pulizia develop/ dopo sync raw/dwh-code
Utente ha risincronizzato `raw/dwh-code/` dalla repo live. Verificati gli 11 file presenti in `develop/`: 9 risultavano identici alla versione ora in `raw/dwh-code/` (portati upstream), i restanti 2 (`cnt_campagna.sql`, `variazioni_anagrafiche.sql`) avevano fix diverse/più complete rispetto a quanto proposto in `develop/`. Confermato dall'utente: eliminati tutti gli 11 file e le directory vuote sotto `develop/`.

## [2026-07-14] ingest | Caricamento layer L0-L1
Ingerito `raw/Agos X - Caricamento layer L0-L1.docx`. Creata pagina sources/caricamento-layer-l0-l1.md e concetti collegati (storicizzazione L1, cancellazioni, naming, cobol-parsing).

## [2026-07-14] ingest | Caricamento layer L2
Ingerito `raw/Agos X - Caricamento layer L2.docx`. Creata pagina sources/caricamento-layer-l2.md e concetti collegati (storicizzazione L2 S1-S4, progressivo, data masking, naming).

## [2026-07-14] ingest | Guida Sviluppo
Ingerito `raw/guida_sviluppo.docx`. Creata pagina sources/guida-sviluppo.md, incluso pattern S5 (scd2_foto_mensile) e variante S1 senza PK propria.

## [2026-07-14] ingest | Agos X - Layer L2.xlsx (reference, non ingest completo)
Su decisione utente, la xlsx (~180 fogli) non è stata ingerita foglio per foglio: creata sources/layer-l2-xlsx-reference.md che ne descrive la struttura (nomenclature, Catalogo Entità, Catalogo Categorie Campi) da consultare puntualmente in query future.

## [2026-07-14] lint | Cross-check documentazione vs codice raw/dwh-code
Lanciati 3 agenti di ricerca in parallelo su raw/dwh-code/ (macros; models/L2; models/L0-L1/snapshots/templates/tests). Scritte 13 pagine entities/ (repo + 4 layer + 12 aree L2), 8 pagine concepts/ (macro-catalogo-dbt, storicizzazione L1/L2/L3, cancellazioni, naming-convention, progressivo, data-masking, cobol-parsing). Filata queries/inconsistenze-doc-vs-codice.md con 11 inconsistenze verificate (query_tag mancante/errato, doppio meccanismo cancellazioni, FL_ values S/N vs Y/N, prefissi campo divergenti, dbt_artifacts non presente, nomi test custom diversi, macro remove_datamask/decode_overpunch non trovate come documentate, doppia implementazione S1, sentinelle DATE/TIMESTAMP miste, bug minori nel codice).

## [2026-07-20] query | rifatta null-vs-placeholder-ocs con stato attuale, dopo re-sync raw/dwh-code

Riletta per intero la nuova sezione 5.5 di `guida_sviluppo.docx` (invariata nel contenuto normativo rispetto al giro precedente: stesse 3 regole, stessa unica eccezione BACCPTES/Poste Italiane, nessuna eccezione aggiuntiva citata). Riverificato uno per uno contro `raw/dwh-code/` (risincronizzato) ogni caso del vecchio inventario. Risultato: solo `ANAGR_CONTROPARTE/legame_ditte_individuali.sql` risulta corretto (usa `custom_is_null()`/`custom_is_not_null()`); tutti gli altri 15 casi IS NULL e 3 casi COALESCE restano aperti (con piccoli shift di numero di riga in `pratica_m.sql` e `dm_controlli_basilea_m.sql`). `wfl_attivita.sql` non è più un caso applicabile: non contiene più un check IS NULL (gestito via CASE su valore esplicito). Riscritta interamente [[null-vs-placeholder-ocs]] senza storico, solo stato presente, con sezioni "Da fare"/"Già corretto"/"Esclusi".
