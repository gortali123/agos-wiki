version: 2

sources:
  - name: source_l0
    schema: L0
    tables:
      - name: oxiafcompe
        data_tests:
          - try_cast
          - primary_key:
              arguments:
                pk_columns: [comp_procedura, comp_contratto, comp_q_cartol, comp_versione, comp_tipo_importo, comp_sotto_tipo, comp_anno]
      - name: oxiafcompe_deleted
