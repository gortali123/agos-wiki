version: 2

sources:
  - name: source_l0
    schema: L0
    tables:
      - name: aiecfpart
        data_tests:
          - try_cast_table:
              arguments:
                rtrim_varchar: true
          - primary_key_table:
              arguments:
                pk_columns: [aiecpart_sezione, aiecpart_pratica, aiecpart_progressivo]
                rtrim_varchar: true
      - name: aiecfpart_deleted
