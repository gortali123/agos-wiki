version: 2

sources:
  - name: source_l0
    schema: L0
    tables:
      - name: aictfpdpl
        data_tests:
          - try_cast
          - primary_key:
              arguments:
                pk_columns: [aictpdp_ide, aictpdp_ide, aictpdp_prg_log, aictpdp_prg_log]
      - name: aictfpdpl_deleted
