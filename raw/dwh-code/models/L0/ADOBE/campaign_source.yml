version: 2

sources:
  - name: source_l0
    schema: L0
    tables:
      - name: campaign
        data_tests:
          - try_cast_table
          - primary_key_table:
              arguments:
                pk_columns: [campaignid]
